<network>
  <!-- lab-lan — /24 isolated, no DHCP (dhcp01 VM handles it) -->
  <name>lab-lan</name>
  <bridge delay="0" name="virbr-lan" stp="on"></bridge>
  <ip address="10.0.1.254" netmask="255.255.255.0"></ip>
  <dns enable="no"></dns>
</network>
